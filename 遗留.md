我理解一下，你的概念中DeviceSpec是设备级schema，包括设备有哪些参数，信号接口，连接接口，流转接口；SceneDocument是场景级schema，包括场景设备连接关系，信号接口关系，物料流转路径。RuntimeSnapshot（运行时的设备信号状态）存储在缓存中。初始值存储，运行时实时变更状态。agent负责根据这些信息制定simplan,runtime编译simplan成可执行的描述，包括当前流程需要的条件，输出的结果（信号）。DeviceRuntimeProfile初始时候是场景级描述，描述设备可执行的行为，但是随着RuntimeSnapshot的变化，DeviceRuntimeProfile也需要变化，来实时描述当前状态下设备可执行的操作

可能有个误解，我详细描述一下我期待的流程，首先运输托盘到第一条传送带末端，然后两个机械臂进行分拣（没有按序号分类）就是直接抓取直到抓完，分拣过程中两个机械臂和对应传送带都是持续运行，机械臂持续抓取物料到传送带上，传送带持续运行。所以基于这个场景下，末端传送带会直到传送带上的承载超过最大承载能力发送block给对应机械臂，该机械臂停下手中动作，直到传送带承载减小。并且在分拣过程中，机械臂没有抓取顺序，谁空闲谁去抓物料，所以可能需要机械臂之间建立信号传递，或者说直接通过全局派发信号？稍等，所以数据库中是有一份基于signalSchema规范的全局设备状态，根据信号的接收来变更设备当前的状态？，所以在simplan中要有基于设备当前状态对应的可执行行为，当前行为执行完毕派发信号变更状态！所以是不是使用全局信号更好一些，毕竟这个全局信号可以包含设备间信号传递？

用户的描述：运行该场景的场景流水线
LLM需要根据场景解析出的行为：首先运输托盘到第一条传送带末端，然后两个机械臂进行分拣（没有按序号分类）就是直接抓取直到抓完，分拣过程中两个机械臂和对应传送带都是持续运行，机械臂持续抓取物料到传送带上，传送带持续运行。
agent首先要将理解之后的行为展示给用户，用户确认再继续

实际运行时，场景快照中有一部分负责存储当前场景的设备信号状态，包括初始值存储，运行时实时变更状态。信号规范受到signalBusSchema的约束,实时值通过signalBusRuntime方法模块/调度模块来传递，根据信号变更设备状态，并且每个设备应该是从快照里获取自己当前的状态；接口处触发信号传递

所以我理解下来：场景快照主要就是负责信号处理和设备状态变更，signalBusRuntime来负责传递值，那么设备就需要根据接受到的信号值来触发对应的行为，并且更改对应的状态，所以实际上快照里还要存储的一份内容是设备接受到的信号所对应的要做的行为？，这个内容需要根据当前的仿真计划以及涉及设备的可执行行为以及对应信号口来推断？
对于调度器的具象化，以当前场景为例，初始传送带空闲时，派发release_waiting_material信号给第一条传送带，第一条传送带根据快照里该信号定义好的行为（运输行为）开始执行运输，状态变为moving，运输完毕后状态变为idle,发送done信号。调度器发现了done信号，了解到应该到下一阶段的行为了，为下一阶段的两个机械臂派发start_pick信号，执行过程中机械臂状态变更为busy并发送busy信号给快照（但是谁来接受这个busy信号应该是根据实际的场景来考虑，这个场景下貌似没有需要消费这个busy信号的地方，调度器会根据现有未处理的物料数量以及机械臂状态发送start_pick信号），并且机械臂第一次done之后，末端的传送带就开始运行了，调度器给末端传送带开始发送信号，实时计算传送带负载，要是超载了，传送带释放block信号，那么在这次的快照中，block信号会触发机械臂的stop行为，或者说直接触发锁，变更状态。但是传送带是持续运输的，并且当负载小于阈值之后，再发送continue信号？机械臂解锁。传送带一直到当前传送带上的最后一个物料运输完之后并且工作台已经没有要运输的物料之后才会发出done信号。

所以我这样看下来，simplan实际上是一个分模块的构建出来的一个场景流水线流程图，每个模块都有自己的任务，任务之间通过信号传递来协调。而且在这个业务场景下，模块之间是依次执行，每个模块执行完毕后才会开始下一个模块。但是在更多业务场景下，每个模块是并行同时运行。所以进一步对于仿真建模的理解，可能是根据实际的业务场景去决定该场景下设备的信号接口处应该发出接受什么样的信号值，而不是只根据我们定义好的信号值

| 用户描述的是目标，不一定描述完整工艺流程 | Agent 需要解释目标、补全合理模块、暴露假设。 |
| 同一设备在不同场景下可能启用不同信号和状态 | 设备能力与场景行为必须解耦。 |
| 并行、持续运行、反馈控制场景难以用线性步骤表达 | 需要事件总线、状态模型和策略函数。 |
| 多机器人抢料、传送带超载、资源互斥、死锁等问题跨场景复用 | 需要统一抽象为状态变量 + 策略函数 + 事件反馈。 |
| Agent 生成的行为模型需要可审计、可复现、可执行 | 需要结构化 `SceneBehaviorGraph` 和校验器。 |

| 节点                     | 输入                       | 输出                                            | 作用                                                   |
| ------------------------ | -------------------------- | ----------------------------------------------- | ------------------------------------------------------ |
| `IntentParserNode`       | 用户自然语言目标           | 工艺目标、阶段、约束、指标、用户假设            | 把“想怎么仿真”转成结构化意图。                         |
| `SceneUnderstandingNode` | `SceneDocument`            | 设备实例、物料、显式连接、可达路径、候选模块    | 理解场景事实，不做隐式连接推断。                       |
| `DeviceCapabilityNode`   | 相关 `DeviceSpec`          | 接口、信号口、行为能力、资源、容量、设备约束    | 建立“可用能力集合”。                                   |
| `ProcessDecomposerNode`  | 意图 + 场景 + 能力         | 业务模块 DAG / 并行区间 / 持续运行模块          | 将目标拆成托盘运输、并行分拣、出料传送等模块。         |
| `EventStateModelerNode`  | 模块 + 设备信号 + 场景连接 | `event_bus`、`state_model` 草案                 | 设计本场景实际需要的事件、信号、状态变量和 payload。   |
| `BehaviorRuleNode`       | 事件状态模型 + 设备行为    | `behavior_rules`、`state_transition_rules` 草案 | 定义事件/条件如何触发行为、行为如何更新状态。          |
| `PolicySynthesizerNode`  | 资源问题 + 业务模块        | `policies`、异常观测策略                        | 组合共享工件池、backpressure、资源锁、死锁检测等策略。 |
| `ValidationNode`         | 行为图草案 + 上游事实      | 校验结果、修复建议                              | 校验引用存在性、状态一致性、路由闭环和策略可判定性。   |
| `ExplanationNode`        | 校验后的行为图             | 用户可读调度理解说明                            | 在提交前展示 Agent 对场景调度的理解。                  |

| 工具                       | 输入                         | 输出                                | 说明                                                                                     |
| -------------------------- | ---------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------- |
| `SceneReader`              | `scene_id`、`scene_revision` | `SceneDocument`、实例索引、连接索引 | 读取场景事实和显式连接。                                                                 |
| `DeviceSpecReader`         | `spec_id`、版本约束          | `DeviceSpec` 能力摘要               | 按场景实例引用读取设备能力。                                                             |
| `GraphValidator`           | `SceneBehaviorGraph` 草案    | 校验错误、警告、修复建议            | 校验设备、接口、信号、行为、状态变量引用。                                               |
| `PolicyLibrary`            | 策略名称 + 场景参数          | 策略模板或策略片段                  | 提供可复用 shared-pool-claim、capacity-backpressure、resource-lock、deadlock-detection。 |
| `SceneBehaviorGraphWriter` | 校验后的行为图               | 持久化记录                          | 写入 `SceneBehaviorGraph`，绑定版本和来源。                                              |
| `ExplanationRenderer`      | 行为图                       | 自然语言解释、事件 trace 预览       | 生成用户可读说明和论文演示材料。                                                         |

- eventBus定义的事件注册、路由和投递规则执行是一个抽象概念，我想知道它具体的运作机制是什么样的，它底层是以什么形式呈现的，处理信号传递的底层逻辑是什么，是类似于vue的eventBus还是说一种发布订阅模式，具体使用什么工具或者方法来实现的，帮我理清楚
信号传递由 SignalBusRuntime 处理。
ActionExecutor 执行动作
  -> 动作完成或状态变化产生 effects
  -> effects 里包含 emit event
  -> SignalBusRuntime.publish(event)
  -> 校验 event 是否注册
  -> 校验 payload
  -> 根据 event_bus.routes 找到投递目标
  -> 写入事件队列 / signal_values / runtime event stream
  -> Scheduler 下一轮读取事件和状态
  -> 触发新的 behavior_rules

例如：
main_conveyor_1.transport_to_exit 完成
  -> emit main_conveyor_1.pallet_ready

SignalBusRuntime 收到 main_conveyor_1.pallet_ready
  -> 路由给 parallel_robot_sorting.enable
  -> Scheduler 看到分拣模块 enabled
  -> 两个 robot 开始根据 policy claim 物料

所以：
RuntimeSnapshot.signal_values
  只保存“当前某个信号的最新值”。

SignalBusRuntime
  才负责“把信号从发送方投递到接收方”。

eventQueue / Redis Streams
  保存“信号/事件流动过程”。

实现方式：走多进程、前端推送、运行回放：
Redis Streams
  保存运行时事件流，例如 XADD sim:run:{run_id}:events

Redis Hash / RedisJSON
  保存 RuntimeSnapshot、signal_values、resource_locks

WebSocket
  从 Redis Streams 或 Runtime 事件流推送给前端

Postgres
  保存 SceneBehaviorGraph、Agent run、关键事件摘要


- Scheduler 找到满足 trigger + guard + policy 的 behavior_rules，trigger + guard + policy是啥，在哪一阶段产生的
它们是 behavior_rules 里的三个层次。
trigger
  什么事情发生后，规则被激活。

guard
  当前状态是否允许执行。

policy
  如果有多个选择，如何决策。

举例：
{
  "rule_id": "robot_claim_and_pick",
  "trigger": {
    "event": "robot.pick_request"
  },
  "guard": [
    "pallet_1.remaining_parts.count > 0",
    "robot.state == idle",
    "target_conveyor.blocked == false"
  ],
  "policy": "claim_workpiece_and_choose_target_conveyor",
  "then_start_behavior": {
    "instance_id": "$robot_id",
    "behavior_id": "pick_and_place"
  }
}
trigger：比如收到 main_conveyor_1.pallet_ready、robot.pick_request、output_conveyor.capacity_available。
guard：比如 robot 必须 idle、工件池非空、目标传送带未 blocked、夹爪资源未占用。
policy：比如谁空闲谁 claim、选择负载更低的传送带、资源冲突时谁优先。

EventStateModelerNode
  -> 生成 event_bus 和 state_model

BehaviorRuleNode
  -> 生成 trigger / guard / then_start_behavior

PolicySynthesizerNode
  -> 生成 policy 引用和 policy 参数

ValidationNode
  -> 校验 trigger、guard、policy 引用是否合法


- Scheduler 为候选行为申请 resource_locks资源锁，资源锁的运作机制是什么样的，类似于数据库的锁机制？
resource_locks 是仿真语义里的资源互斥锁。
它有点像操作系统锁，也有点像数据库锁，但不是为了数据库隔离，而是为了保证仿真动作不会非法并发。
例如：
robot_1.gripper
  同一时刻只能被一个 pick_and_place action 占用。

main_conveyor_1.belt_surface
  同一时刻只能执行一个主要 transport action。

pallet_1.slot_001
  同一物料槽位不能被两个 robot 同时 claim。

运行机制类似这样：
Scheduler 找到候选行为
  -> 检查行为需要哪些 resources
  -> 尝试 acquire resource_locks
  -> 全部申请成功才启动 action
  -> 写入 RuntimeSnapshot.resource_locks
  -> action complete / fail / cancel 后 release locks

快照里可能长这样：
{
  "resource_locks": {
    "robot_1.gripper": {
      "owner_action_id": "action_robot_1_pick_part_003",
      "mode": "exclusive",
      "acquired_at": 12.5
    },
    "upper_out_conveyor_1.belt_surface": null
  }
}

如果单进程 Runtime，锁可以是内存里的 Map。
如果多进程 Runtime，建议用 Redis 原子操作：

- 调度算法是不是应用在Runtime loop的循环行为中，并且对于当前的调度算法建议，“规则驱动 + 策略函数 + 确定性优先级”的调度算法，现在是否有已有的可用来改造适配这次系统的调度算法？
是的。
调度算法就是 Runtime loop 中 Scheduler 的核心逻辑。
每一轮大概是：
1. 读取 RuntimeSnapshot 当前状态
2. 读取上一轮产生的新事件
3. 找到 trigger 命中的 behavior_rules
4. 检查 guard 是否满足
5. 执行 policy 做动态决策
6. 申请 resource_locks
7. 启动 action
8. action 产生 effects
9. SignalBusRuntime 投递事件
10. SnapshotManager 更新 RuntimeSnapshot
所以调度算法不是 Agent 阶段执行的。Agent 只是生成调度规则，Runtime 的 Scheduler 才真正调度。

现成的调度算法
已有方法	适合借鉴的部分
离散事件仿真 DES	Runtime loop、事件队列、仿真时钟、事件驱动执行。
Rule Engine	trigger + guard -> action 的规则匹配。
Resource-Constrained Project Scheduling	多设备、多资源互斥时的调度选择。
Petri Net	token / transition 思想适合做可达性和死锁检测。
Behavior Tree	适合表达设备局部行为，但不适合作为整个场景总模型。
HTN / GOAP	适合 Agent 规划任务分解，但 Runtime 阶段不建议过重。

可用工具方向：
Node / TypeScript:
  json-rules-engine：可借鉴规则判断，但资源调度要自己扩展。
  XState：适合设备 FSM，不适合整个场景事件总线。
  BullMQ / Redis Streams：适合事件队列和异步任务流。
  async-mutex：适合单进程锁。
  Redis SET NX / Lua：适合分布式资源锁。

Python:
  SimPy：典型离散事件仿真框架，适合参考 DES 机制。
  durable_rules：规则引擎思路可参考。

建议是不要直接依赖一个重型调度框架，而是实现一个领域化轻量 Scheduler：
规则匹配：自己实现
事件队列：内存队列 / Redis Streams
guard 表达式：JSONLogic / CEL / 自定义 DSL
policy：Runtime 内置 PolicyLibrary
资源锁：内存锁 / Redis 原子锁
设备 FSM：可参考 XState 思路

- 我看到eventbus中定义了事件和路由，信号的传递是在哪来着？快照里存的应该是状态值吧，那么信号传递谁来处理来着以及策略函数应该存在哪
策略定义
  存在 SceneBehaviorGraph.policies。

策略实现
  存在 Runtime 的 PolicyLibrary 代码里。


最终总结
Agent 生成 SceneBehaviorGraph：
  定义事件、路由、规则、状态、策略。

Runtime 初始化 RuntimeSnapshot：
  保存当前设备、物料、信号、锁和动作状态。

Scheduler 每轮读取 SceneBehaviorGraph + RuntimeSnapshot：
  用 trigger 找规则，
  用 guard 判断能不能执行，
  用 policy 做动态选择，
  用 resource_locks 保证资源互斥，
  然后启动 action。

ActionExecutor 执行动作：
  产生 effects 和 emit events。

SignalBusRuntime：
  负责把 emit 出来的事件按 event_bus.routes 投递出去。

SnapshotManager：
  把事件和动作结果更新回 RuntimeSnapshot。



  理解一下，在调度生成中
  调度生成的behavior_rules中，此次仿真涉及到的每一个行为都会给出一套对应的rule
  state_transition_rules是对应不同行为触发什么状态的变更，主要通过effects来告诉SignalBusRuntime该触发哪些状态的变更