# SceneBehaviorGraph 运行机制理解

## 1. 信号、行为、事件、状态之间的关系

### 1.1 事件 `event`

事件是某个瞬间发生的事实，例如：

```text
runtime.sim_start
robot.pick_done
conveyor.blocked
```

事件通常用于唤醒 `behavior_rules`，也可以作为状态迁移或异常观测的触发源。

### 1.2 信号 `signal`

信号是设备之间或 Runtime 内部通信的语义接口。

在 Runtime 中，信号通常会被包装成 event 来传递。信号的最新值可以保存在：

```text
RuntimeSnapshot.signal_values
```

需要注意：

```text
SignalBusRuntime 负责信号 / 事件投递；
RuntimeSnapshot.signal_values 只保存某些信号的最新状态值。
```

### 1.3 行为 `behavior`

行为是设备可执行能力，来自：

```text
DeviceSpec.transport_behaviors
```

例如：

```text
conveyor.transport_to_exit
robot.pick_and_place
```

当 `behavior_rules[].action.type = start_behavior` 时，Runtime 会根据：

```text
action.instance_id
action.behavior_id
```

启动某个场景设备实例上的某个设备行为。

### 1.4 状态 `state`

状态是当前持续存在的事实，保存在：

```text
RuntimeSnapshot
```

例如：

```text
device_states.robot_1 = idle
conveyor_loads.current_load = 3
resource_locks.robot_1.gripper = null
```

---

## 2. `SceneBehaviorGraph` 中各部分的职责

### 2.1 `event_bus`

`event_bus` 是 Agent 基于用户需求、场景事实和设备能力推导出的事件 / 信号定义。

它描述该场景下可能发生的：

```text
事件
设备信号
控制事件
异常观测 observation
```

其中：

```text
event_bus.events
  定义有哪些事件、payload 长什么样、来源是什么。

event_bus.routes
  定义事件产生后应该投递给谁，例如 rule、module、device、topic 或 Runtime 内部模块。
```

需要注意：

```text
event_bus 只是定义；
SignalBusRuntime 才是真正执行事件投递的 Runtime 模块。
```

### 2.2 `state_model`

`state_model` 定义 `RuntimeSnapshot` 需要维护的状态变量结构。

它不只包含设备状态，也包含场景级状态，例如：

```text
device_states
signal_values
workpiece_pool
conveyor_loads
resource_locks
active_actions
```

边界是：

```text
SceneBehaviorGraph.state_model
  定义状态结构。

RuntimeSnapshot
  保存某次 run 中这些状态的初始值和实时值。
```

### 2.3 `behavior_rules`

运行时某个 event 出现后，Scheduler 会根据 `event_id` 找到 `trigger` 匹配的 `behavior_rules`。

关系不是一一对应：

```text
一条 event 可以唤醒多条 behavior_rules；
一条 behavior_rule 也可以由 event、state_change、scheduler_tick 等不同 trigger 类型唤醒。
```

`behavior_rules` 描述：

```text
某类 trigger 发生后，
满足 guard 和 policy 时，
应执行什么 action。
```

其中：

```text
trigger
  规则何时被唤醒。

guard
  当前状态是否允许执行。

policy
  动态策略，例如优先级、claim、负载均衡、backpressure、资源互斥。

action
  最终要执行的动作，例如 start_behavior、emit_event、update_state。
```

当 `action.type = start_behavior` 时：

```text
action.instance_id
  指向场景中的设备实例。

action.behavior_id
  指向该设备在 DeviceSpec.transport_behaviors 中声明的可执行行为。
```

### 2.4 `state_transition_rules`

行为开始、完成、失败或某个事件发生后，会命中对应的 `state_transition_rules`。

一类行为生命周期事件可以对应多条 `state_transition_rules`。

每条 `state_transition_rules.effects` 描述该行为或事件带来的：

```text
状态变更
新事件发射
```

需要区分两类 effect：

```text
状态变更 effect
  例如设置设备状态、移动物料位置、更新负载、申请或释放资源锁。
  由 EffectApplier / SnapshotManager 执行。

事件发射 effect
  例如 emit main_conveyor_1.pallet_ready。
  由 SignalBusRuntime 根据 event_bus.routes 投递。
```

所以不是：

```text
effects 告诉 SignalBusRuntime 触发状态变更。
```

而是：

```text
EffectApplier 解析 effects；
状态变更交给 SnapshotManager；
事件发射交给 SignalBusRuntime。
```

---

## 3. 完整运行链路

1. Agent 生成 `SceneBehaviorGraph`。

2. `SceneBehaviorGraph.event_bus` 定义该场景可能发生的事件、设备信号、控制事件和路由关系。

3. `SceneBehaviorGraph.state_model` 定义 `RuntimeSnapshot` 需要维护的状态变量结构，例如：

   ```text
   device_states
   signal_values
   workpiece_pool
   resource_locks
   active_actions
   ```

4. Runtime 初始化 `RuntimeSnapshot`，写入设备初始状态、物料位置、资源锁、工件池等当前值。

5. Runtime 收到或产生某个 event，例如：

   ```text
   runtime.sim_start
   ```

6. `SignalBusRuntime` 根据 `event_bus.routes` 投递 event。

7. `Scheduler` 根据 `event_id` 找到 `trigger` 匹配的 `behavior_rules`。

8. `Scheduler` 检查 `guard`，判断当前 `RuntimeSnapshot` 是否满足执行条件。

9. `Scheduler` 调用 `policy`，完成优先级排序、claim、目标选择、backpressure、资源互斥等动态决策。

10. `Scheduler` 申请 `resource_locks`。

11. `Scheduler` 执行 `action`，例如：

    ```text
    start_behavior main_conveyor_1.transport_to_exit
    ```

12. `ActionExecutor` 启动设备行为，并把 action 写入：

    ```text
    RuntimeSnapshot.active_actions
    ```

13. 行为开始、完成、失败或事件发生后，命中 `state_transition_rules`。

14. `EffectApplier` 执行 effects：

    ```text
    状态变更 -> SnapshotManager
    emit event -> SignalBusRuntime
    ```

15. `RuntimeSnapshot` 更新后，进入下一轮 Runtime loop。

---

## 4. 一句话总结

```text
event_bus 定义事件/信号和路由；
state_model 定义 RuntimeSnapshot 要维护哪些状态；
behavior_rules 定义 trigger 命中后如何判断并执行 action；
action 可启动 DeviceSpec 中声明的 behavior；
state_transition_rules 定义行为/事件产生的 effects；
EffectApplier 拆解 effects；
SnapshotManager 更新状态；
SignalBusRuntime 投递新事件。
```
