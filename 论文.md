# 自然语言驱动的场景行为仿真研究方向

本文将当前项目的学术研究点收敛为三条主线：完整场景的工艺行为建模、基于 LangGraph 编排的仿真工艺规划 Agent、基于调度算法的实时调度器。三者共同构成从自然语言目标到可执行行为仿真的闭环。

---

## 1. 完整场景的工艺行为建模

### 1.1 研究目标

将用户的自然语言工艺目标、场景中的设备连接关系、设备自身能力统一建模为一个可解释、可校验、可执行的 `SceneBehaviorGraph`。

该建模结果不再拆成多段中间 schema，而是直接描述当前场景的真实运行机制，包括：

- 场景业务模块如何划分。
- 设备之间通过哪些事件和信号协作。
- Runtime 需要维护哪些状态变量。
- 哪些事件会触发哪些行为。
- 行为开始、完成、失败后如何改变状态。
- 如何处理共享资源、容量约束、backpressure、死锁和完成条件。

### 1.2 核心建模对象

`SceneBehaviorGraph` 是当前基线中的最终工艺建模产物，主要包含：

| 板块 | 作用 |
|---|---|
| `goal` | 描述用户目标、场景假设和建模边界。 |
| `modules` | 描述场景中的业务模块，例如托盘运输、并行分拣、出料传送。 |
| `event_bus` | 描述本场景中所有事件、设备信号、控制事件、异常观测和事件路由。 |
| `state_model` | 描述 RuntimeSnapshot 需要维护的状态变量，例如设备状态、物料池、资源锁、负载、active actions。 |
| `behavior_rules` | 描述事件到达后，在什么前置条件和策略下触发什么设备行为。 |
| `state_transition_rules` | 描述行为开始、完成、失败或事件发生后如何修改运行时状态，并是否继续发出新事件。 |
| `policies` | 描述动态决策函数，例如共享工件池 claim、容量阈值、资源锁、死锁检测。 |
| `completion_conditions` | 描述整条仿真任务完成的判定条件。 |
| `failure_observations` | 描述超载、死锁、资源冲突、物料异常等观测事件。 |

### 1.3 研究问题

传统仿真系统通常依赖人工脚本或固定流程图描述工艺行为，但自然语言驱动的柔性仿真需要解决以下问题：

1. 如何把自然语言目标转成结构化、可执行的行为图。
2. 如何让同一套设备在不同业务目标下形成不同的事件、状态和协作逻辑。
3. 如何表达并行、持续运行、反馈控制、资源互斥和动态选择。
4. 如何让行为模型既能被 Agent 生成，也能被 Runtime 稳定执行。
5. 如何保证建模结果可解释、可校验、可复现。

### 1.4 学术创新点

- **自然语言到场景行为图的结构化映射**：将自然语言工艺意图转化为 `SceneBehaviorGraph`，使工艺仿真从脚本编排转向模型生成。
- **设备能力与场景行为解耦**：`DeviceSpec` 只描述设备原生能力，`SceneBehaviorGraph` 描述当前场景实际使用哪些事件、状态和策略。
- **事件总线中心化建模**：使用 `event_bus + behavior_rules + state_transition_rules` 统一表达设备信号、全局事件、控制事件和异常观测。
- **连续过程的离散事件化**：连续运行设备不持续发事件，而是在状态跨越关键边界时产生离散事件，例如 blocked、capacity_available、sorting_done。
- **可解释行为建模**：每个事件、路由、规则、策略和状态变更都具有明确语义，便于用户确认和系统校验。

---

## 2. 基于 LangGraph 编排的仿真工艺规划 Agent

### 2.1 研究目标

构建一个可解释、可验证的多阶段 Agent，用于从 `DeviceSpec + SceneDocument + 用户目标` 生成 `SceneBehaviorGraph`。

该 Agent 不应一次性黑盒生成完整 JSON，而应通过多个节点逐步完成意图理解、场景理解、设备能力抽取、流程分解、事件建模、规则生成、策略合成和结果校验。

### 2.2 推荐节点设计

| 节点 | 输入 | 输出 | 作用 |
|---|---|---|---|
| `IntentParserNode` | 用户自然语言目标 | 工艺目标、约束、假设、性能指标 | 理解用户到底希望场景如何运行。 |
| `SceneUnderstandingNode` | `SceneDocument` | 设备实例、物料、显式连接、可达路径 | 理解场景中有什么、怎么连接。 |
| `DeviceCapabilityNode` | `DeviceSpec` | 设备接口、行为、信号口、资源、容量约束 | 理解设备能做什么、不能做什么。 |
| `ProcessDecomposerNode` | 用户目标、场景理解、设备能力 | `modules` | 将工艺目标拆成托盘运输、并行分拣、出料传送等业务模块。 |
| `EventStateModelerNode` | modules、设备信号口、用户目标 | `event_bus`、`state_model` | 生成本场景需要的事件、信号、状态变量和 payload。 |
| `BehaviorRuleNode` | event_bus、state_model、设备行为 | `behavior_rules`、`state_transition_rules` | 定义事件如何触发行为、行为如何改变状态。 |
| `PolicySynthesizerNode` | 资源约束、并发关系、容量约束 | `policies` | 生成共享工件池、backpressure、资源锁、死锁检测等策略。 |
| `ValidationNode` | 初版 `SceneBehaviorGraph` | 校验报告、修订建议 | 校验引用完整性、状态变量完整性、事件路由闭环和策略可执行性。 |
| `ExplanationNode` | 校验后的 `SceneBehaviorGraph` | 用户可读解释 | 在提交前向用户展示 Agent 对场景调度的理解。 |

### 2.3 Agent 需要的工具

| 工具 | 作用 |
|---|---|
| `SceneReader` | 读取 `SceneDocument`，获取设备实例、物料、显式连接、位姿和拓扑关系。 |
| `DeviceSpecReader` | 按 `spec_id` 查询设备能力，包括接口、信号口、行为、资源和运行契约。 |
| `GraphValidator` | 校验实例、接口、信号、行为、状态变量和路由引用是否存在。 |
| `PolicyLibrary` | 提供可复用策略模板，例如 shared-pool-claim、capacity-backpressure、resource-lock、deadlock-detection。 |
| `SceneBehaviorGraphWriter` | 输出结构化 `SceneBehaviorGraph` JSON。 |
| `ExplanationRenderer` | 将行为图转换为用户可读说明，用于确认 Agent 的调度理解。 |

### 2.4 上下文与记忆

基线版本优先使用短期上下文：

- 当前用户目标。
- 当前 `scene_id` 和 `scene_revision`。
- 当前场景引用的 `DeviceSpec` 版本。
- 本次 Agent 生成过程中的中间摘要、校验错误和修订记录。

长期记忆暂不参与 Runtime 决策，只用于保存可复用建模模式，例如：

- 分拣线模板。
- 共享工件池模板。
- 出料 backpressure 模板。
- 多设备资源锁模板。
- 死锁检测模板。

每次生成 `SceneBehaviorGraph` 时，必须绑定 `scene_id`、`scene_revision` 和 `device_spec_versions`，避免场景或设备规范变更后继续使用旧行为图。

### 2.5 Agent 交互机制

Agent 在生成最终结果前，应先向用户展示自己对场景调度的理解，例如：

- 场景中有哪些设备和物料。
- 设备之间有哪些显式连接。
- 计划拆成哪些业务模块。
- 哪些模块顺序执行，哪些模块并行或持续运行。
- 哪些事件会触发关键行为。
- 哪些状态变量会被 Runtime 维护。
- 哪些异常情况需要被观测，例如超载、死锁、资源冲突。

用户确认后，Agent 再生成或修订 `SceneBehaviorGraph`。

### 2.6 学术创新点

- **多阶段可解释规划 Agent**：通过 LangGraph 节点化拆解建模流程，避免一次性黑盒生成不可控 schema。
- **工具增强的行为图生成**：Agent 不是凭空生成，而是通过 SceneReader、DeviceSpecReader、GraphValidator 和 PolicyLibrary 约束输出。
- **生成-校验-解释闭环**：将 LLM 生成、结构化校验和用户可读解释组合成闭环，提高模型可信度。
- **可复用工艺模式记忆**：长期记忆不直接控制 Runtime，而是沉淀可复用行为建模模板，降低复杂场景建模成本。

---

## 3. 基于调度算法的实时调度器

### 3.1 研究目标

构建 Runtime Scheduler，使其能够读取 `SceneBehaviorGraph + RuntimeSnapshot`，在离散事件仿真循环中稳定选择可执行行为、处理信号传递、推进设备动作并更新状态。

这里的重点不是重新规划工艺，而是可靠执行已经生成的行为模型。

### 3.2 Runtime 模块分工

| 模块 | 职责 |
|---|---|
| `RunInitializer` | 根据 `SceneBehaviorGraph.state_model` 和 `SceneDocument.materials` 初始化 `RuntimeSnapshot`。 |
| `SignalBusRuntime` | 校验事件、匹配路由、展开 topic 订阅、投递事件。 |
| `Scheduler` | 找到满足 `trigger + guard + policy` 的 `behavior_rules`，决定当前要执行哪些行为。 |
| `PolicyExecutor` | 执行 `policies` 中定义的策略函数，例如 claim、backpressure、资源锁、死锁检测。 |
| `ActionExecutor` | 执行设备行为，例如传送、抓取、放置、持续运输。 |
| `SnapshotManager` | 按 `state_transition_rules` 写回设备状态、物料位置、资源锁、信号值和 active actions。 |
| `ObservationEmitter` | 当超载、死锁、资源冲突、超时等条件成立时产生 observation 事件。 |

### 3.3 离散事件仿真循环

推荐采用单线程离散事件仿真作为基线：

```text
Runtime 初始化 RuntimeSnapshot
  -> emit runtime.sim_start
  -> SignalBusRuntime 投递事件
  -> Scheduler 查找被唤醒的 behavior_rules
  -> guard 校验状态条件
  -> PolicyExecutor 执行动态策略
  -> ActionExecutor 启动或推进设备行为
  -> SnapshotManager 应用 state_transition_rules
  -> 连续状态跨阈值时 emit 新事件
  -> 进入下一轮 Runtime loop
```

单线程并不表示场景里不能有多个设备同时运行，而是指 Runtime 用一个确定性的事件队列推进多个并发设备状态。这样可以保证仿真可复现、方便调试，也能避免真实多线程锁带来的不确定性。

### 3.4 调度算法建议

当前基线建议采用：**规则驱动 + 策略函数 + 确定性优先级**。

调度过程可以拆成：

1. **事件唤醒**：根据事件 ID 找到候选 `behavior_rules`。
2. **状态过滤**：执行 `guard`，过滤掉当前状态不满足的规则。
3. **资源检查**：检查 `resource_locks`，确保同一设备、夹爪、物料、传送带资源不会被冲突占用。
4. **策略决策**：执行 policy，例如共享工件池 claim、目标传送带选择、backpressure 判断。
5. **确定性排序**：如果同一 tick 存在多个候选行为，按 `deterministic_priority` 或固定 tie-breaker 排序。
6. **动作启动**：启动设备行为并写入 `active_actions`。
7. **状态推进**：行为完成或状态跨阈值时触发 `state_transition_rules`。

该调度算法可以吸收已有离散事件仿真、规则引擎和资源调度思想，但在本系统中需要面向 `SceneBehaviorGraph` 做适配：规则来源于 Agent 生成，策略函数来源于可校验模板，状态事实来自 `RuntimeSnapshot`。

### 3.5 资源调度与异常处理

不同场景下的资源问题统一抽象为：**状态变量 + 策略函数 + 事件反馈**。

| 问题 | 状态变量 | 策略函数 | 反馈事件 |
|---|---|---|---|
| 多机器人抢同一物料 | `workpiece_pool`、`material_claims` | `shared_pool_claim` | `global.workpiece_claimed`、`observation.claim_conflict_detected` |
| 出料传送带超载 | `conveyor_loads` | `capacity_threshold` / `backpressure` | `output_conveyor.blocked`、`output_conveyor.capacity_available` |
| 设备资源互斥 | `resource_locks` | `resource_lock` | `observation.resource_conflict` |
| 长时间无可执行行为 | `active_actions`、任务剩余状态 | `deadlock_detection` | `observation.deadlock_detected` |
| 动作超时 | `active_actions`、时间戳 | `timeout_retry` | `observation.action_timeout` |

### 3.6 连续过程如何触发离散事件

设备可以持续运行，但 Runtime 不需要每一帧都生成业务事件。推荐方式是：

```text
连续动作推进状态
  -> RuntimeSnapshot 中的负载、位置、动作进度变化
  -> monitor / policy 检查是否跨越关键阈值
  -> 只有跨越边界时 emit 离散事件
```

例如出料传送带：

```text
current_load 从 4 增加到 5
max_capacity = 5
blocked = false
  -> emit output_conveyor.blocked
  -> set blocked = true
```

后续如果 `current_load` 仍然是 5，不重复发 blocked。只有状态从 `not blocked` 进入 `blocked` 时才发事件。

恢复时：

```text
current_load 从 4 降到 3
resume_threshold = 3
blocked = true
  -> emit output_conveyor.capacity_available
  -> set blocked = false
```

### 3.7 学术创新点

- **Agent 生成行为图的可执行调度**：把 LLM 输出从静态说明转为 Runtime 可解释执行的行为规则和状态迁移。
- **事件驱动的多设备协同仿真**：使用事件总线统一设备信号、全局事件、控制事件和异常观测。
- **确定性离散事件调度**：用单线程事件队列模拟多设备并发，提高复现性和可调试性。
- **策略函数化资源调度**：把死锁、超载、共享物料池、资源锁等问题抽象成可组合策略函数。
- **连续过程的事件边界检测**：将连续设备状态变化通过阈值、完成条件和 monitor 转换为离散事件，兼顾真实过程和可执行建模。

---

## 4. 三条研究主线的关系

```text
自然语言目标 + DeviceSpec + SceneDocument
  -> LangGraph 仿真工艺规划 Agent
  -> SceneBehaviorGraph
  -> Runtime Scheduler / SignalBusRuntime / ActionExecutor
  -> RuntimeSnapshot 状态演化
  -> 仿真结果、事件日志、异常观测、报告输出
```

三条主线分别解决不同层面的问题：

| 主线 | 核心问题 | 核心产物 |
|---|---|---|
| 完整场景的工艺行为建模 | 如何表达场景真实运行机制 | `SceneBehaviorGraph` |
| 基于 LangGraph 的仿真工艺规划 Agent | 如何自动生成行为图 | Agent 工作流、工具链、校验闭环 |
| 基于调度算法的实时调度器 | 如何可靠执行行为图 | Runtime loop、事件队列、策略执行、状态更新 |

这三条主线合起来构成当前项目的核心学术框架：

> 面向自然语言驱动工业仿真的可解释场景行为图生成与事件驱动调度方法。
